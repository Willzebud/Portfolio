/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com"],
  },
  env: {
    domaine: "https://wr-portfolio-268f1ff6ebd6.herokuapp.com",
  },
};

export default nextConfig;
